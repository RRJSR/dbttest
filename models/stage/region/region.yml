version: 2
models:
  - name: region
    description: "{{ doc('REGION_docs') }}"
    database: RAW
    schema: STAGE
    
    columns:
      - name: R_REGIONKEY
        tests:
          - not_null
          - unique