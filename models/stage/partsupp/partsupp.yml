version: 2
models:
  - name: partsupp
    description: "{{ doc('PARTSUPP_docs') }}"
    database: RAW
    schema: STAGE
    
    columns:
      - name: PS_PARTKEY
        tests:
          - not_null
      - name: PS_SUPPKEY
        tests:
          - not_null
       
          
          